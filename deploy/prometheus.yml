global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "rusaren-server"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "rarena-server:3000"
